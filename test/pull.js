var Docker = require('dockerode');

describe('pull', function() {
  it('should pull an image if it has older tags than the registry', function() {

  });
  it('should pull an image if it is an ancestor of the currently tagged image', function() {

  });
});